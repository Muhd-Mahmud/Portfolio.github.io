# Lab 01 — AMR Foundations assets

Drop the captured evidence here. Filenames are referenced from
`src/data/lab-projects.tsx`; until a file exists the page renders a labelled
placeholder instead of a broken image.

| File | Content |
| --- | --- |
| `cad.png` | Onshape CAD model of the differential-drive robot |
| `gazebo.png` | Robot spawned in the Gazebo simulation environment |
| `rviz.png` | Robot description visualized in RViz |
| `tf-tree.png` | TF frame tree (screenshot of the `view_frames` output) |
| `tf-tree.pdf` | TF frame tree, downloadable artifact (`ros2 run tf2_tools view_frames`) |

Images render at 16:9 — export around 1600×900 and keep each under ~400 KB.
